# Copy to .env for local dev. On Heroku these are config vars.
ANTHROPIC_API_KEY=
TOKEN_MINT_ADDRESS=
TREASURY_ADDRESS=
BURN_ADDRESS=                 # unused: the burn is a real SPL burn, not a transfer
OPS_ADDRESS=
DEPOSIT_MASTER_SEED=          # 64 hex chars. KMS in production. Never in the repo.
RPC_URL=
DATABASE_URL=              # Heroku sets this; locally: postgres://derek:derek@localhost:55432/derek
FEE_TARGET_USD=0.40
MIN_LIQUIDITY_USD=15000
MAX_DAILY_API_USD=25
MAX_DAILY_X_USD=5             # X is $0.015 a post; stops a loop burning credits
MAX_SUBMISSIONS_PER_HOUR=400
CLAIM_EXPIRY_DAYS=7
AUTO_APPROVE_UNFLAGGED=false  # keep false for the first ~100 rulings
PAUSED=true
X_CONSUMER_KEY=               # OAuth 1.0a: all four needed to post
X_CONSUMER_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
X_USER_ID=                    # optional; only to reconcile a failed post
SWEEP_FEE_PAYER_SECRET=      # base58 secret key with a little SOL; pays sweep fees
SWEEP_DUST_TOKENS=1           # balances at or below this are written off
TOKEN_DECIMALS=9
CHAIN_ID=solana
FAKE_TREASURY_USD=            # pre-launch only: overrides on-chain treasury valuation
PORT=3000
